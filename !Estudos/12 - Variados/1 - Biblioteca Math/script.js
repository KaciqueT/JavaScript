const math = document.getElementById("math")

//a biblioteca matematica de JS é enorme

const num = Math.random()*10 

// math.innerHTML = Math.floor(num)// - numero aleatorio de 0 - 9
// math.innerHTML = Math.round(num)// - numero aleatorio de 0 - 10
// math.innerHTML = Math.sqrt(9)// - raiz quadrada
// math.innerHTML = Math.pow(10, 2)// - potencia
// math.innerHTML = Math.floor(num)+1// - numero aleatorio de 1 - 10